// Tailwind v4 is handled by the @tailwindcss/vite plugin (see vite.config.ts),
// so no PostCSS plugins are needed here. This empty config exists to stop
// PostCSS from searching parent directories for a config.
export default {};
